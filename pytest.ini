[pytest]
# The INTEGRATION suite. The agent has its own, under agent/, with its own
# config: the two run on different Python versions against different
# dependencies, and a single collection root would drag Home Assistant into a
# suite whose whole point is that it needs nothing.
testpaths = tests
asyncio_mode = auto
addopts = -q
