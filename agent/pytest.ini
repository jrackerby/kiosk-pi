[pytest]
testpaths = tests
# The package is not installed; it is copied. Tests import it from the source
# tree the same way the systemd unit does, so nothing here depends on a build
# step the Pi will never run.
pythonpath = .
addopts = -q
